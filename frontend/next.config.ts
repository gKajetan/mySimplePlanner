// gkajetan/mysimpleplanner/mySimplePlanner-dev-stand/frontend/next.config.ts

const nextConfig = {
  // 1. Set the base path to your new URL slug.
  // DO NOT use "/reglament-system" here, use the new name.
  basePath: "/my-simple-planner",

  reactStrictMode: true,
  // output: 'standalone', // Optional: Recommended for Docker deployments
};

module.exports = nextConfig;
